{% extends 'base.html.twig' %}

{% block title %}New Countries{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="card">
            <div class="card-header">
                <h1 class="mb-0">Create new Country</h1>
            </div>
            <div class="card-body">
                {{ include('countries/_form.html.twig') }}
            </div>
            <div class="card-footer">
                <a href="{{ path('app_countries_index') }}" class="btn btn-secondary">Back to List</a>
            </div>
        </div>
    </div>
{% endblock %}
